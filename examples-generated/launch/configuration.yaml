apiVersion: launch.aws.jet.crossplane.io/v1alpha1
kind: Configuration
metadata:
  name: example
spec:
  forProvider:
    imageId: ${data.aws_ami.ubuntu.id}
    instanceType: t2.micro
    name: web_config
  providerConfigRef:
    name: example
