apiVersion: kms.aws.jet.crossplane.io/v1alpha1
kind: ExternalKey
metadata:
  name: example
spec:
  forProvider:
    description: KMS EXTERNAL for AMI encryption
  providerConfigRef:
    name: example
