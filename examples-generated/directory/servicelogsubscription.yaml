apiVersion: directory.aws.jet.crossplane.io/v1alpha1
kind: ServiceLogSubscription
metadata:
  name: example
spec:
  forProvider:
    directoryId: ${aws_directory_service_directory.example.id}
    logGroupName: Yada
  providerConfigRef:
    name: example
