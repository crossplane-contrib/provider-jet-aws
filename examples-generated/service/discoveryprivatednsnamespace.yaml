apiVersion: service.aws.jet.crossplane.io/v1alpha1
kind: DiscoveryPrivateDnsNamespace
metadata:
  name: example
spec:
  forProvider:
    description: example
    name: hoge.example.local
    vpc: ${aws_vpc.example.id}
  providerConfigRef:
    name: example
