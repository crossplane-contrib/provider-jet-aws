apiVersion: msk.aws.jet.crossplane.io/v1alpha1
kind: ScramSecretAssociation
metadata:
  name: example
spec:
  forProvider:
    clusterArn: ${aws_msk_cluster.example.arn}
    depends_on:
    - ${aws_secretsmanager_secret_version.example}
    secretArnList:
    - ${aws_secretsmanager_secret.example.arn}
  providerConfigRef:
    name: example
