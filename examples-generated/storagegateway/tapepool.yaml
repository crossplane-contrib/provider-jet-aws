apiVersion: storagegateway.aws.jet.crossplane.io/v1alpha1
kind: TapePool
metadata:
  name: example
spec:
  forProvider:
    poolName: example
    storageClass: GLACIER
  providerConfigRef:
    name: example
