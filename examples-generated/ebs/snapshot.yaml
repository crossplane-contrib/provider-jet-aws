apiVersion: ebs.aws.jet.crossplane.io/v1alpha1
kind: Snapshot
metadata:
  name: example
spec:
  forProvider:
    tags:
      Name: HelloWorld_snap
    volumeId: ${aws_ebs_volume.example.id}
  providerConfigRef:
    name: example
