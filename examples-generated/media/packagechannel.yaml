apiVersion: media.aws.jet.crossplane.io/v1alpha1
kind: PackageChannel
metadata:
  name: example
spec:
  forProvider:
    channelId: kitten-channel
    description: A channel dedicated to amusing videos of kittens.
  providerConfigRef:
    name: example
