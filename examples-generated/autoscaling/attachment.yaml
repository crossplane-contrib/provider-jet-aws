apiVersion: autoscaling.aws.jet.crossplane.io/v1alpha1
kind: Attachment
metadata:
  name: example
spec:
  forProvider:
    autoscalingGroupNameRef:
      name: example
    elb: ${aws_elb.bar.id}
  providerConfigRef:
    name: example
