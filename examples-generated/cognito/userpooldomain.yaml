apiVersion: cognito.aws.jet.crossplane.io/v1alpha1
kind: UserPoolDomain
metadata:
  name: example
spec:
  forProvider:
    domain: example-domain
    userPoolId: ${aws_cognito_user_pool.example.id}
  providerConfigRef:
    name: example
