apiVersion: cognito.aws.jet.crossplane.io/v1alpha1
kind: UserGroup
metadata:
  name: example
spec:
  forProvider:
    description: Managed by Terraform
    name: user-group
    precedence: 42
    roleArn: ${aws_iam_role.group_role.arn}
    userPoolId: ${aws_cognito_user_pool.main.id}
  providerConfigRef:
    name: example
