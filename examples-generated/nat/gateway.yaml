apiVersion: nat.aws.jet.crossplane.io/v1alpha1
kind: Gateway
metadata:
  name: example
spec:
  forProvider:
    allocationId: ${aws_eip.example.id}
    depends_on:
    - ${aws_internet_gateway.example}
    subnetId: ${aws_subnet.example.id}
    tags:
      Name: gw NAT
  providerConfigRef:
    name: example
