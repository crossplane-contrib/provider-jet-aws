apiVersion: ec2.aws.jet.crossplane.io/v1alpha1
kind: CarrierGateway
metadata:
  name: example
spec:
  forProvider:
    tags:
      Name: example-carrier-gateway
    vpcId: ${aws_vpc.example.id}
  providerConfigRef:
    name: example
