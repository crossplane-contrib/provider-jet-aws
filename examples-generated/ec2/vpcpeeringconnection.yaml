apiVersion: ec2.aws.jet.crossplane.io/v1alpha1
kind: VPCPeeringConnection
metadata:
  name: example
spec:
  forProvider:
    peerOwnerId: ${var.peer_owner_id}
    peerVpcIdRef:
      name: example
    vpcIdRef:
      name: example
  providerConfigRef:
    name: example
