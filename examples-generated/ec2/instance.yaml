apiVersion: ec2.aws.jet.crossplane.io/v1alpha1
kind: Instance
metadata:
  name: example
spec:
  forProvider:
    ami: ${data.aws_ami.ubuntu.id}
    instanceType: t3.micro
    tags:
      Name: HelloWorld
  providerConfigRef:
    name: example
