apiVersion: ec2.aws.jet.crossplane.io/v1alpha1
kind: TransitGatewayRouteTable
metadata:
  name: example
spec:
  forProvider:
    transitGatewayIdRef:
      name: example
  providerConfigRef:
    name: example
