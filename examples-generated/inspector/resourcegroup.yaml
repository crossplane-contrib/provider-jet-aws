apiVersion: inspector.aws.jet.crossplane.io/v1alpha1
kind: ResourceGroup
metadata:
  name: example
spec:
  forProvider:
    tags:
      Env: bar
      Name: foo
  providerConfigRef:
    name: example
