apiVersion: backup.aws.jet.crossplane.io/v1alpha1
kind: GlobalSettings
metadata:
  name: example
spec:
  forProvider:
    globalSettings:
      isCrossAccountBackupEnabled: "true"
  providerConfigRef:
    name: example
