apiVersion: lakeformation.aws.jet.crossplane.io/v1alpha1
kind: Resource
metadata:
  name: example
spec:
  forProvider:
    arn: ${data.aws_s3_bucket.example.arn}
  providerConfigRef:
    name: example
