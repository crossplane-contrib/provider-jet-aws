apiVersion: waf.aws.jet.crossplane.io/v1alpha1
kind: RateBasedRule
metadata:
  name: example
spec:
  forProvider:
    depends_on:
    - ${aws_waf_ipset.ipset}
    metricName: tfWAFRule
    name: tfWAFRule
    predicates:
    - dataId: ${aws_waf_ipset.ipset.id}
      negated: false
      type: IPMatch
    rateKey: IP
    rateLimit: 100
  providerConfigRef:
    name: example
