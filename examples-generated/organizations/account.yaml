apiVersion: organizations.aws.jet.crossplane.io/v1alpha1
kind: Account
metadata:
  name: example
spec:
  forProvider:
    email: john@doe.org
    name: my_new_account
  providerConfigRef:
    name: example
