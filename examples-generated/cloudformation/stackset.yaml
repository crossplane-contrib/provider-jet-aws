apiVersion: cloudformation.aws.jet.crossplane.io/v1alpha1
kind: StackSet
metadata:
  name: example
spec:
  forProvider:
    administrationRoleArn: ${aws_iam_role.AWSCloudFormationStackSetAdministrationRole.arn}
    name: example
    parameters:
      VPCCidr: 10.0.0.0/16
    templateBody: |
      {
        "Parameters" : {
          "VPCCidr" : {
            "Type" : "String",
            "Default" : "10.0.0.0/16",
            "Description" : "Enter the CIDR block for the VPC. Default is 10.0.0.0/16."
          }
        },
        "Resources" : {
          "myVpc": {
            "Type" : "AWS::EC2::VPC",
            "Properties" : {
              "CidrBlock" : { "Ref" : "VPCCidr" },
              "Tags" : [
                {"Key": "Name", "Value": "Primary_CF_VPC"}
              ]
            }
          }
        }
      }
  providerConfigRef:
    name: example
