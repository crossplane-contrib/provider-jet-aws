apiVersion: iot.aws.jet.crossplane.io/v1alpha1
kind: Certificate
metadata:
  name: example
spec:
  forProvider:
    active: true
    csr: ${file("/my/csr.pem")}
  providerConfigRef:
    name: example
