apiVersion: fsx.aws.jet.crossplane.io/v1alpha1
kind: LustreFileSystem
metadata:
  name: example
spec:
  forProvider:
    importPath: my-tf-test-bucket
    storageCapacity: 1200
    subnetIds:
    - ${aws_subnet.example.id}
  providerConfigRef:
    name: example
