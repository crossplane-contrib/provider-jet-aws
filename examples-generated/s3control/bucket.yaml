apiVersion: s3control.aws.jet.crossplane.io/v1alpha1
kind: Bucket
metadata:
  name: example
spec:
  forProvider:
    bucket: example
    outpostId: ${data.aws_outposts_outpost.example.id}
  providerConfigRef:
    name: example
