apiVersion: athena.aws.jet.crossplane.io/v1alpha1
kind: Database
metadata:
  name: example
spec:
  forProvider:
    bucket: my-tf-test-bucket
    name: database_name
  providerConfigRef:
    name: example
