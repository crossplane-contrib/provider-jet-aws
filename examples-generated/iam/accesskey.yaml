apiVersion: iam.aws.jet.crossplane.io/v1alpha1
kind: AccessKey
metadata:
  name: example
spec:
  forProvider:
    pgpKey: keybase:some_person_that_exists
    userRef:
      name: example
  providerConfigRef:
    name: example
