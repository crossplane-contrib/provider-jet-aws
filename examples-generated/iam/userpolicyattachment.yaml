apiVersion: iam.aws.jet.crossplane.io/v1alpha1
kind: UserPolicyAttachment
metadata:
  name: example
spec:
  forProvider:
    policyArnRef:
      name: example
    userRef:
      name: example
  providerConfigRef:
    name: example
