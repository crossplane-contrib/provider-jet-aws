apiVersion: iam.aws.jet.crossplane.io/v1alpha1
kind: SamlProvider
metadata:
  name: example
spec:
  forProvider:
    name: myprovider
    samlMetadataDocument: ${file("saml-metadata.xml")}
  providerConfigRef:
    name: example
