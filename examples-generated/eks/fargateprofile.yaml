apiVersion: eks.aws.jet.crossplane.io/v1alpha1
kind: FargateProfile
metadata:
  name: example
spec:
  forProvider:
    clusterNameRef:
      name: example
    podExecutionRoleArnRef:
      name: example
    selector:
    - namespace: example
    subnetIdRefs:
      name: example
  providerConfigRef:
    name: example
