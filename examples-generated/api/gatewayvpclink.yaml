apiVersion: api.aws.jet.crossplane.io/v1alpha1
kind: GatewayVpcLink
metadata:
  name: example
spec:
  forProvider:
    description: example description
    name: example
    targetArns:
    - ${aws_lb.example.arn}
  providerConfigRef:
    name: example
