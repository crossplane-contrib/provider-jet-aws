apiVersion: api.aws.jet.crossplane.io/v1alpha1
kind: GatewayRequestValidator
metadata:
  name: example
spec:
  forProvider:
    name: example
    restApiId: ${aws_api_gateway_rest_api.example.id}
    validateRequestBody: true
    validateRequestParameters: true
  providerConfigRef:
    name: example
