apiVersion: dax.aws.jet.crossplane.io/v1alpha1
kind: SubnetGroup
metadata:
  name: example
spec:
  forProvider:
    name: example
    subnetIds:
    - ${aws_subnet.example1.id}
    - ${aws_subnet.example2.id}
  providerConfigRef:
    name: example
