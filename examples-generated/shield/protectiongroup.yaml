apiVersion: shield.aws.jet.crossplane.io/v1alpha1
kind: ProtectionGroup
metadata:
  name: example
spec:
  forProvider:
    aggregation: MAX
    pattern: ALL
    protectionGroupId: example
  providerConfigRef:
    name: example
