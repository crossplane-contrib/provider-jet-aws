apiVersion: opsworks.aws.jet.crossplane.io/v1alpha1
kind: NodejsAppLayer
metadata:
  name: example
spec:
  forProvider:
    stackId: ${aws_opsworks_stack.main.id}
  providerConfigRef:
    name: example
