apiVersion: pinpoint.aws.jet.crossplane.io/v1alpha1
kind: SmsChannel
metadata:
  name: example
spec:
  forProvider:
    applicationId: ${aws_pinpoint_app.app.application_id}
  providerConfigRef:
    name: example
