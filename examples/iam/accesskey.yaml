apiVersion: iam.aws.tf.crossplane.io/v1alpha1
kind: IamAccessKey
metadata:
  name: testkey
spec:
  forProvider:
    region: ""
    userRef:
      name: testuser
  writeConnectionSecretToRef:
    name: testkey-secret
    namespace: crossplane-system
